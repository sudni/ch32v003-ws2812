[build]
target = "riscv32ec-unknown-none-elf.json"

[unstable]
build-std = ["core"]
build-std-features = ["compiler-builtins-mem"]
json-target-spec = true

[target.riscv32ec-unknown-none-elf]
runner = "wlink --chip CH32V003 flash"
rustflags = [
    "-C", "link-arg=-Tlink.x",
    "--emit=asm",
]
